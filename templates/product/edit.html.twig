{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | Modifier l'article {{ product.name }}{% endblock %}

{% block body %}
    <a class="btn btn-primary" href="{{ path('admin') }}">Retour à la liste</a>
    <main class="mx-auto w-75">
        <h1>Modifier le produit</h1>
        {% include 'product/_form.html.twig' %}
    </main>

{% endblock %}
